import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com", "m.media-amazon.com", "randomuser.me"],
  },
};

export default nextConfig;
